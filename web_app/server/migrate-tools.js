/**
 * This script is a one-time migration tool to convert tool specs from the old format 
 * into the new format used by the server. You only need to run this once.
 */
console.log('Migration script has been deprecated as we no longer use the tool_specs directory.');
console.log('All tools are now stored in server/data/tools.json');
console.log('If you need to run a migration, please restore this script from version control.');
